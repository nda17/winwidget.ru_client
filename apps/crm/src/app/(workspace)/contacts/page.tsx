import { ContactsScreen } from '@/screens/contacts'
import type { Metadata } from 'next'

export const metadata: Metadata = {
	title: 'Контакты'
}

const ContactsPage = () => <ContactsScreen />

export default ContactsPage
